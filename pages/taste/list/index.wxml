<view class="container">
	<view class="taste-list">
		<view class="taste-item" wx:for="{{projects}}" bindtap="pushActivityDetailView">
			<view class="taste-title">{{item.title}}</view>
			<image src="{{item.coverUrl}}" class="taste-cover"></image>
		</view>
	</view>
</view>
